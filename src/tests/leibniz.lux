

var pi = 1;
var x = 1;
var start_time = clock();

for( var i = 2; i < 100000000 +2 ;i= i+1) {
    x = x* -1;
    pi= pi + (x/(2*i-1));
}

print("Result:"+ pi*4);
print("End Time:"+ clock()-start_time);
